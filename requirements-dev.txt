pip-audit>=2.9.0,<3.0.0
pytest>=8.4.0,<10.0.0
ruff>=0.16.3,<1.0.0

# Bridge runtime dependency, needed so the bridge test suite imports under the
# repository-wide pytest run.
-r bridge/requirements.txt

